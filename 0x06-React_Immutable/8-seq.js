import { Seq } from "immutable";

function printBestStudents(grades) {
    const grades = {
    1: {
        score: 99,
        firstName: "guillaume",
        lastName: "salva",
    },
};
}

export default printBestStudents;
